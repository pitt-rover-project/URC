services:
  pitt_urc_local:
    image: urc_local
    container_name: pitt_urc_local
    stdin_open: true # Equivalent to `-it`
    tty: true # Keeps the container interactive
    restart: unless-stopped
    network_mode: bridge
    ports:
      - "7400-7500:7400-7500/udp"
      - "11888:11888"
    pid: host
    ipc: host
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
      - ROS_DOMAIN_ID=0
      - ROS_LOCALHOST_ONLY=0
      - RMW_IMPLEMENTATION=rmw_fastrtps_cpp
      - FASTRTPS_DEFAULT_PROFILES_FILE=/app/docker/local/fastdds_profile.xml
    volumes:
      - ./../../:/app
      - /tmp/.X11-unix:/tmp/.X11-unix
